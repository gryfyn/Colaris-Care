{
  "name": "colaris-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Colaris — a healthcare operating system. One login, one database. Care module.",
  "workspaces": [
    "Care/web"
  ],
  "scripts": {
    "care:dev": "npm run dev --workspace=@colaris/care-web",
    "care:build": "npm run build --workspace=@colaris/care-web",
    "care:test": "npm run test --workspace=@colaris/care-web"
  },
  "engines": {
    "node": ">=20"
  }
}
