// Staff use the same DB-connected appointments page as admin (SAFETY_WRITE), so
// they can view and schedule appointments against the shared facility schedule.
export { default } from "../../admin/appointments/page";
