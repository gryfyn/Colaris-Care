import ContactForm from './ContactForm';

export const metadata = {
  title: 'Contact — Colaris',
  description:
    'Get in touch with the Colaris team at Glass Inc Technologies — sales, support, or general questions.',
};

export default function ContactPage() {
  return <ContactForm />;
}
