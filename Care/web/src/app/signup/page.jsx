import SignupForm from "./SignupForm";

export const metadata = {
  title: "Create your account — Colaris",
  description: "Start a new facility on Colaris. Sign up with email to set up your admin and staff workspaces.",
};

export default function SignupPage() {
  return <SignupForm />;
}
